/* === sample 9 ==== */

int f(int n) {
	if(n < 2) {
		return 1;
	} else {
		return f(n - 1) + f(n - 2);
	} endif
}

void main(void){
	int n;
	n = 0;
	repeat {
		output(f(n));
		n = n + 1;
	} until(n == 10)
}